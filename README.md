# xcodegen-v1.4.0-trouble
```
$ bundle install
$ xcodegen
$ bundle exec pod install
$ open RswiftWithXcodeGen.xcworkspace
# Build app then show error↓
# [R.swift] Project file at 'file:///path/to/RswiftWithXcodeGen.xcodeproj/' could not be parsed, is this a valid Xcode project file ending in *.xcodeproj?
```
